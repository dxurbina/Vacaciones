<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    DirectoryIndex seo.php index.php

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /seo.php [L]
</IfModule>
